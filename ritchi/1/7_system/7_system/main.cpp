#include<iostream>

using namespace std;

int main()
{
	//cout<<system("dir");//run system var
	//cout<<getenv("PATH");//return path values
	cout<<"run"<<endl;
	//exit(0);
	abort();
	system("pause");
	return 0;
}